using UnityEngine;

namespace Interactables.Gun
{
    public class HUDIndicatorManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}