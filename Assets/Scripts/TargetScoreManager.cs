using UnityEngine;

public class TargetScoreManager : PrefabCacheCompatible
{
    // Start is called before the first frame update
    void Start()
    {
        Destroy();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}